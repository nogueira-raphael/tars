"""TARS MCP protocol adapter.

Deliberately thin — no domain layer here. If you find yourself wanting one,
the logic belongs in `tars_core` instead. See docs/adr/0010 and AGENTS.md
at the repository root.
"""
