"""Pure domain layer: entities, value objects, aggregates, and ports.

No imports from `tars_core.application` or `tars_core.infrastructure`, and no
imports from any framework or database driver. See docs/adr/0010 at the
repository root for why this boundary is enforced.
"""
