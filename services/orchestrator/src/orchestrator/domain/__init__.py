"""Pure domain layer for the Conversation bounded context. No imports from
`orchestrator.application` or `orchestrator.infrastructure`, and no imports
from `tars_core` (see the package-level docstring)."""
