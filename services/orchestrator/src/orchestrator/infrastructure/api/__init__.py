"""FastAPI routes + the SSE endpoint the Web UI consumes.

TODO: the exact endpoint/event/payload contract is deliberately not designed
yet — see docs/architecture/overview.md's open items. Design it here, once
`orchestrator`'s domain/application layers exist, not before.
"""
